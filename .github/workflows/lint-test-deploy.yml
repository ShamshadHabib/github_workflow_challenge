name: first workflow

on: push

jobs:
  testDeploy:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v7
      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: 20
      - name: build dependencies
        run: npm ci
      - name: lint
        run: |
          echo "Running lint"
          npm run lint
          echo "test done"
      - name: test
        run: |
          echo "Running test"
          npm run test
          echo "test done"
      - name: deploy
        run: npm run build
      
